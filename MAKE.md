# Comandos úteis
- `make uv-setup` : cria venv com uv e instala deps
- `make lint`     : ruff + black check
- `make test`     : pytest
- `make run-batch`: roda avaliação em lote (CLI)
- `make serve`    : sobe FastAPI local
- `make dk-up`    : docker compose (app + mlflow)
